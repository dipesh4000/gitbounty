{
  "name": "gitbounty-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/points.js && node --check src/content.js && node --test tests/*.test.js"
  }
}
