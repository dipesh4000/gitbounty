@AGENTS.md

## This is inside the GitBounty repo

Read the repository's own docs before changing anything here: [`../../overview.md`](../../overview.md) for what the
product is, [`../../rules.md`](../../rules.md) for the hard rules (read the git history first, commit every small
step), and [`../../feature-split.md`](../../feature-split.md) for who owns which feature.

This app is a port of the static landing page in [`../web/`](../web). That page is still live and still the one
being served; this is the Next.js version of it.
